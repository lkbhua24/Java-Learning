package com.lkbhua.InnerClassDemo.demo3;

public class Test {
    public static void main(String[] args) {
        /*
        局部内部类
        1、将内部类定义在方法里面就叫做局部内部类，类似于方法里的局变量
        2、外界是无法直接使用局部内部类的，必须通过外部类对象创建局部内部类对象
        3、该类可以直接访问外部类的成员变量和成员方法，也可以访问方法内部的局部变量和局部方法
        * */
        Outer o = new Outer();
        o.show();

    }
}
