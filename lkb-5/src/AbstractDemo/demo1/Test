package com.lkbhua.AbstractDemo.demo1;

public class Test {
    public static void main(String[] args) {
        // 1、抽象类不能创建对象
        // Person p = new Person();

        /*回答疑问：5、抽象类不能创建对象，但是可以通过抽象类创建抽象类的子类对象，引用的是抽象类的成员变量
        虽然不能直接创建抽象类对象，但是可以通过抽象类创建抽象类的子类对象，引用的是抽象类的成员变量
        调用抽象类成员变量和成员方法，但是成员方法必须重写！*/
        Student s = new Student("lkbhua", 18);
        s.work();
        System.out.println(s.getName()+" "+s.getAge());

    }
}
